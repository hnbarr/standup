import React from 'react'
// import PropTypes from 'prop-types'
import { Typography } from '@material-ui/core'

const Progress = props => {
    return (
        <div>
            <Typography>im a progress circle, well the area its gonna be anyways.</Typography>
        </div>
    )
}

// Progress.propTypes = {

// }

export default Progress

// racf functional component es6 shorthand
